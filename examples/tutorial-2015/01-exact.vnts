assume x = normal(0, 1)
observe normal(x, 1) = 2
infer
  do(resample(10),
     d <- accumulate_dataset(1000,
            do(rejection(default, all, 1),
               collect(x))),
     plotf(quote(h0), d))
